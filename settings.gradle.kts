rootProject.name = "RozproszenieAplikacji"

// Spring Boot 4.x jest jeszcze w fazie RC – wymaga repozytorium milestone
pluginManagement {
    repositories {
        maven("https://repo.spring.io/milestone")
        gradlePluginPortal()
    }
}
